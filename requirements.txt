# Deploy Streamlit Community Cloud — CHỈ dependency cho streamlit_app/app.py.
# Pipeline dữ liệu / model đầy đủ: xem requirements-dev.txt
streamlit>=1.30,<1.50
pandas>=2.1,<2.3          # 2.x nhẹ RAM hơn 3.0 (pyarrow-string mặc định) — quan trọng cho free tier
numpy>=1.24,<2.2
pyarrow>=14,<18
plotly>=5.18,<6
